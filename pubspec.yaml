name: random_eats_plus
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # スプラッシ画面
  flutter_native_splash: ^1.1.8+4

dev_dependencies:
  flutter_test:
    sdk: flutter

# スプラッシュ画面設定
flutter_native_splash:
  image: "assets/splash/no_image.jpg"
  color: "ffffff"

flutter:

  uses-material-design: true