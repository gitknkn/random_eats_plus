import 'package:flutter/material.dart';

class AllEatsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('一覧画面'),
    );
  }
}
